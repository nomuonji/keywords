{
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "apps/api/dist/server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/apps/api/dist/server.js"
    }
  ]
}
